#include "main.h"

/**
 * _abs - check for alphabetic number n
 * @n: The character to check
 *Description : using the _abs function
 *Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
		return (-n);
}
