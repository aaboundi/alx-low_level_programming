#include "main.h"

/**
 * _islower - check for lowercase character c
 * @c: The character to check
 *Description : using the _islower function
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
